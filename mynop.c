#include "monty.h"

/**
 * myNop - Does nothing
 * @start: A pointer to the head of the stack
 * @iterator: The line number where the opcode appears
 *
 */
void myNop(stack_t **start, unsigned int iterator)
{
	(void)start;
	(void)iterator;
	/* This function does nothing as nop opcode implies */
}
